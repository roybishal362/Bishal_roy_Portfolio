# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit secrets
.env
.env.local
.env*.local
.env.*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# raw source assets (WhatsApp memoji originals) — processed copies live in public/memoji
.assets-src/

# local reference clone of the motivation repo — study only, never ships
yuvi-portfolio/

# local working files - not needed to run the site
/docs/
/certificates/
/mockups/
